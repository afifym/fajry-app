export const featureFlags = {
  /** Fajr & Sunrise ticks and icon badges on the sleep/wake clock ring. */
  clockPrayerTimeHighlights: false,
  /** Settings row to pick Makkah / Madinah / Mishary adhan audio. */
  adhanRecitationSetting: false,
} as const;
